@page "/teacher-search"

@using System.Linq;
@using Models;
@using DBL;
@inject IJSRuntime js
@using Microsoft.AspNetCore.Components.Server.ProtectedBrowserStorage

@inject ProtectedSessionStorage MySession
@inject Microsoft.AspNetCore.Components.NavigationManager navigationManager
@using Microsoft.AspNetCore.SignalR.Client


<HeadContent>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teacher Search</title>
   
</HeadContent>
<div>
    <div class="container">
        <h3>Search for Teachers</h3>
        <form class="search-bar" onsubmit="return false;">

            <button type="button" @onclick="SearchTeachers">Search</button>
        </form>
        <div class="filter-section">
            <label for="subjectFilter">Filter by Subject:</label>
            <select id="subjectFilter" @bind="SelectedSubject">
                <option value="">All Subjects</option>
                @foreach (Subject subject in Subjects)
                {
                    <option value="@subject.subjectid">@subject.subjectName</option>
                }
            </select>
            <label for="hourlyRate">Max Hourly Rate:</label>
            <input type="number" id="hourlyRate" @bind="MaxHourlyRate" min="0">
            <label>
                <input type="checkbox" @bind="IsPrivateOnly"> Private Teachers Only
            </label>
        </div>
        @if (SearchResults?.Any() ?? false)
        {
            <div class="results">
                <h4>Results:</h4>
                <ul>
                    @foreach (Teacher teacher in SearchResults)
                    {
                        <li>
                            <strong>@teacher.first_name @teacher.last_name</strong><br />
                            @*   Subjects: @string.Join(", ", Subjects.Select(s => s.subjectName))<br /> *@
                            Hourly Rate: $@teacher.hourly_rate<br />
                            <a href="ViewTeacherProfileAndRequest/@teacher.Id">set a meeting </a>
                        </li>
                    }
                </ul>
                <div class="pagination">
                    @if (CurrentPage > 1)
                    {
                        <button @onclick="() => ChangePage(CurrentPage - 1)">Previous</button>
                    }
                    @if (CurrentPage < TotalPages)
                    {
                        <button @onclick="() => ChangePage(CurrentPage + 1)">Next</button>
                    }
                </div>
            </div>
        }
        else if (SearchExecuted)
        {
            <p>No results found.</p>
        }
    </div>
</div>
<style>
    :root {
        font-size: 16px;
        --primary-color: #3b82f6;
        --secondary-color: #1f2937;
        --background-color: #111827;
        --container-bg: #1e293b;
        --border-color: #374151;
        --text-color: #f9fafb;
        --accent-color: #60a5fa;
    }

    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }

    body {
        background: var(--background-color);
        font-family: Arial, sans-serif;
        color: var(--text-color);
    }

    .container {
        width: 100%;
        max-width: 900px;
        background: var(--container-bg);
        padding: 24px;
        border-radius: 12px;
        margin: 40px auto;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
    }

    h3 {
        text-align: center;
        margin-bottom: 24px;
        color: var(--accent-color);
        font-size: 1.8rem;
    }

    .search-bar {
        display: flex;
        justify-content: center;
        margin-bottom: 24px;
    }

        .search-bar button {
            background: var(--primary-color);
            color: #fff;
            padding: 10px 24px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 1rem;
            transition: background-color 0.3s ease;
        }

            .search-bar button:hover {
                background: var(--accent-color);
            }

    .filter-section {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 16px;
        margin-bottom: 24px;
    }

        .filter-section label {
            font-weight: 600;
            font-size: 0.95rem;
        }

        .filter-section input,
        .filter-section select {
            width: 100%;
            padding: 10px;
            border: 1px solid var(--border-color);
            border-radius: 6px;
            background: var(--background-color);
            color: var(--text-color);
            font-size: 1rem;
        }

            .filter-section input[type="checkbox"] {
                width: auto;
                margin-right: 8px;
            }

    .results {
        margin-top: 24px;
    }

        .results h4 {
            margin-bottom: 16px;
            color: var(--accent-color);
            font-size: 1.4rem;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 8px;
        }

        .results ul {
            list-style: none;
            padding: 0;
        }

        .results li {
            padding: 16px;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

            .results li:last-child {
                border-bottom: none;
            }

        .results a {
            color: var(--accent-color);
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s ease;
        }

            .results a:hover {
                color: var(--primary-color);
            }

    .pagination {
        display: flex;
        justify-content: space-between;
        margin-top: 24px;
    }

        .pagination button {
            background: var(--primary-color);
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 1rem;
            transition: background-color 0.3s ease;
        }

            .pagination button:hover {
                background: var(--accent-color);
            }

    p {
        text-align: center;
        font-size: 1rem;
        color: var(--text-color);
    }
</style>


@code {
    teacherDB TeacherDB=new teacherDB();
    SubjectDB SubjectDB=new SubjectDB();

    private string SearchQuery { get; set; } = string.Empty;
    private string SelectedSubject { get; set; } = string.Empty;
    private int? MaxHourlyRate { get; set; } =0;
    private bool IsPrivateOnly { get; set; } = false;
    private List<Subject> Subjects { get; set; } = new List<Subject>();
    private List<Teacher> SearchResults { get; set; } = new List<Teacher>();
    private List<Teacher> PaginatedResults => SearchResults.Skip((CurrentPage - 1) * PageSize).Take(PageSize).ToList();
    private int CurrentPage { get; set; } = 1;
    private int PageSize { get; set; } = 15;
    private int TotalPages => (int)Math.Ceiling((double)(SearchResults?.Count ?? 0) / PageSize);
    private bool SearchExecuted { get; set; } = false;

    private Student userS = null;
    protected override async Task OnInitializedAsync()
    {
        Subjects = await SubjectDB.SelectAllSubjects();
    }

    private async Task SearchTeachers()
    {
        if(!string.IsNullOrEmpty(SelectedSubject))
        {
        int mysub=0;
     
      if(MaxHourlyRate==0)
      {
        SearchResults= await TeacherDB.GetAllPrivateTeachersForThisSubjectId(int.Parse(SelectedSubject));
      }
      else 
      {
                SearchResults = await TeacherDB.GetAllPrivateTeachersForThisSubjectId(int.Parse(SelectedSubject),int.Parse(MaxHourlyRate.ToString()));
      }
        

        }
        CurrentPage = 1;
        SearchExecuted = true;
    }

    private void ChangePage(int newPage)
    {
        CurrentPage = newPage;
    }
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            var user1 = await MySession.GetAsync<Student>("student");
            if (user1.Success)
            {
                userS = user1.Value;
              
                StateHasChanged();
            }
        }
    }
}
